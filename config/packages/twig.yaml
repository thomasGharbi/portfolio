twig:
    file_name_pattern: '*.twig'
    paths:
        '%kernel.project_dir%/templates/partials': partials
        '%kernel.project_dir%/templates/main_page': main

when@test:
    twig:
        strict_variables: true
